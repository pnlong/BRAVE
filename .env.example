# Copy to .env and fill in ( .env is gitignored ):
#   cp .env.example .env
#
# Used by scripts/*.sbatch via scripts/load_env.sh

WANDB_API_KEY=your_wandb_api_key_here

# Bulk datasets, LMDBs, and training runs (default: /data/hai-res/$USER/BRAVE-data)
# BRAVE_STORAGE=/data/hai-res/p1long/BRAVE-data
